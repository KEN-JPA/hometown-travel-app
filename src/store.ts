import { create } from 'zustand';
import { persist, createJSONStorage } from 'zustand/middleware';
import { kvStorage } from './lib/kvStorage';
export type IconType = 'plane' | 'car' | 'home' | 'building' | 'ticket' | 'map-pin';

export interface Event {
  id: string;
  time: string;
  title: string;
  location: string;
  icon: IconType;
}

export interface DaySchedule {
  id: string;
  date: string;
  events: Event[];
}

export interface TripCategory {
  id: string;
  name: string;
  description?: string;
  schedules: DaySchedule[];
}

export interface Booking {
  id: string;
  category: string;
  icon: IconType;
  provider: string;
  reference: string;
  details: string;
  color: string;
  imageKey?: string;
  link?: string;
}

export interface Expense {
  id: string;
  category: string;
  amount: number;
  color: string;
  isPaid?: boolean;
  description?: string;
}

export interface PackingItem {
  id: string;
  name: string;
  isPacked: boolean;
  category: string;
  assignee?: string;
  quantity?: number;
  description?: string;
}

export interface ShoppingItem {
  id: string;
  name: string;
  isBought: boolean;
  recipient?: string;
  quantity?: number;
  budget?: number;
  description?: string;
}

export interface Memory {
  id: string;
  imageKey: string;
  caption: string;
  date?: string;
  location?: string;
}

export interface PreparationTask {
  id: string;
  title: string;
  category: string;
  targetDate: string | null;
  status: 'pending' | 'in_progress' | 'completed';
  notes: string;
  url?: string;
  isAutoGenerated?: boolean;
}

export interface WishlistItem {
  id: string;
  name: string;
}

export interface Trip {
  id: string;
  tripName: string;
  tripDate: string | null;
  participantsCount?: number;
  itineraryCategories: TripCategory[];
  bookings: Booking[];
  expenses: Expense[];
  packingList: PackingItem[];
  shoppingList: ShoppingItem[];
  memories: Memory[];
  preparationTasks: PreparationTask[];
  wishlist: WishlistItem[];
}

interface TravelStore {
  trips: Trip[];
  selectedTripId: string | null;
  selectTrip: (id: string | null) => void;
  addTrip: (trip: Omit<Trip, 'id' | 'itineraryCategories' | 'bookings' | 'expenses' | 'packingList' | 'shoppingList' | 'memories' | 'preparationTasks' | 'wishlist'>) => void;
  updateTrip: (id: string, tripName: string, tripDate: string | null, participantsCount?: number) => void;
  deleteTrip: (id: string) => void;
  reorderTrips: (startIndex: number, endIndex: number) => void;
  // Methods below act on the selectedTripId
  addCategory: (category: Omit<TripCategory, 'id'>) => void;
  updateCategory: (categoryId: string, name: string, description?: string) => void;
  deleteCategory: (categoryId: string) => void;
  addDaySchedule: (categoryId: string, date: string) => void;
  updateDaySchedule: (categoryId: string, dayId: string, date: string) => void;
  deleteDaySchedule: (categoryId: string, dayId: string) => void;
  addEvent: (categoryId: string, dayId: string, event: Omit<Event, 'id'>) => void;
  updateEvent: (categoryId: string, dayId: string, eventId: string, updates: Partial<Event>) => void;
  deleteEvent: (categoryId: string, dayId: string, eventId: string) => void;
  addBooking: (booking: Omit<Booking, 'id'>) => void;
  deleteBooking: (bookingId: string) => void;
  updateBooking: (bookingId: string, updates: Partial<Booking>) => void;
  updateBookingImage: (bookingId: string, imageKey: string) => void;
  addExpense: (expense: Omit<Expense, 'id'>) => void;
  updateExpense: (expenseId: string, updates: Partial<Expense>) => void;
  deleteExpense: (expenseId: string) => void;
  toggleExpensePaid: (expenseId: string) => void;
  addPackingItem: (item: Omit<PackingItem, 'id' | 'isPacked'>) => void;
  updatePackingItem: (itemId: string, updates: Partial<PackingItem>) => void;
  togglePackingItem: (itemId: string) => void;
  deletePackingItem: (itemId: string) => void;
  addShoppingItem: (item: Omit<ShoppingItem, 'id' | 'isBought'>) => void;
  updateShoppingItem: (itemId: string, updates: Partial<ShoppingItem>) => void;
  toggleShoppingItem: (itemId: string) => void;
  deleteShoppingItem: (itemId: string) => void;
  addMemory: (memory: Omit<Memory, 'id'>) => void;
  updateMemory: (memoryId: string, updates: Partial<Memory>) => void;
  deleteMemory: (memoryId: string) => void;
  addPreparationTask: (task: Omit<PreparationTask, 'id'>) => void;
  updatePreparationTask: (taskId: string, updates: Partial<PreparationTask>) => void;
  deletePreparationTask: (taskId: string) => void;
  generateAutoTasks: () => void;
  addWishlistItem: (name: string) => void;
  deleteWishlistItem: (itemId: string) => void;
}

const generateId = () => Math.random().toString(36).substring(2, 9);

const sampleTrip1: Trip = {
  id: 'trip-1',
  tripName: '✈️ 【サンプル】2026 夏の北海道 家族旅行',
  tripDate: '2026-08-10T00:00:00Z',
  participantsCount: 4,
  itineraryCategories: [
    {
      id: 'cat-1',
      name: '1️⃣ 札幌・小樽 観光編',
      schedules: [
        {
          id: 'day-1',
          date: '8月10日 (月) - 移動と小樽運河',
          events: [
            { id: generateId(), time: '09:00', title: 'ANA 051便 新千歳行き', location: '羽田空港 第2ターミナル', icon: 'plane' },
            { id: generateId(), time: '11:30', title: 'レンタカー借り出し', location: 'ニッポンレンタカー 新千歳', icon: 'car' },
            { id: generateId(), time: '13:00', title: '小樽運河散策 ＆ 海鮮丼', location: '小樽市', icon: 'map-pin' },
            { id: generateId(), time: '17:00', title: 'ホテル チェックイン', location: '小樽グランドホテル', icon: 'building' }
          ]
        }
      ]
    },
    {
      id: 'cat-2',
      name: '2️⃣ 富良野・美瑛 ドライブ編',
      schedules: [
        {
          id: 'day-2',
          date: '8月11日 (火) - ラベンダー畑',
          events: [
            { id: generateId(), time: '10:00', title: 'ファーム富田', location: '中富良野町', icon: 'map-pin' },
            { id: generateId(), time: '14:00', title: '青い池', location: '美瑛町', icon: 'map-pin' }
          ]
        }
      ]
    }
  ],
  bookings: [
    { id: generateId(), category: '飛行機', icon: 'plane', provider: 'ANA (全日空)', reference: 'HOK123', details: 'ANA051 - 羽田 発 / 新千歳 着', color: '#3b82f6' },
    { id: generateId(), category: 'レンタカー', icon: 'car', provider: 'ニッポンレンタカー', reference: 'NR-8888', details: 'ミニバン (家族用)', color: '#10b981' },
    { id: generateId(), category: 'ホテル', icon: 'building', provider: '小樽グランドホテル', reference: 'HTL-777', details: 'ファミリールーム - 1泊', color: '#8b5cf6' }
  ],
  expenses: [
    { id: generateId(), category: '飛行機代 (家族4人)', amount: 120000, color: '#3b82f6', isPaid: true },
    { id: generateId(), category: 'レンタカー代', amount: 35000, color: '#10b981', isPaid: false },
    { id: generateId(), category: 'ホテル宿泊費', amount: 40000, color: '#8b5cf6', isPaid: false }
  ],
  packingList: [
    { id: generateId(), name: '家族分のパスポート・航空券', isPacked: false, category: '重要', assignee: 'パパ', quantity: 4 },
    { id: generateId(), name: 'モバイルバッテリー', isPacked: true, category: 'ガジェット', assignee: 'ママ', quantity: 2 },
    { id: generateId(), name: '防寒着 (夜は冷えるため)', isPacked: false, category: '衣類', quantity: 4 },
    { id: generateId(), name: '常備薬・酔い止め', isPacked: false, category: '日用品' }
  ],
  shoppingList: [
    { id: generateId(), name: '職場用の白い恋人 3箱', isBought: false, recipient: '職場', quantity: 3, budget: 3000 },
    { id: generateId(), name: '両親へ海鮮セット', isBought: false, recipient: '両親', quantity: 1, budget: 10000 }
  ],
  memories: [],
  preparationTasks: [
    { id: generateId(), title: 'JAL特典航空券 予約', category: '飛行機', targetDate: '2025-09-19', status: 'pending', notes: '搭乗日の330日前 9:30開始', isAutoGenerated: true },
    { id: generateId(), title: 'ホテル予約', category: 'ホテル', targetDate: '2026-02-10', status: 'pending', notes: '約6ヶ月前目安', isAutoGenerated: true }
  ],
  wishlist: []
};

export const useTravelStore = create<TravelStore>()(
  persist(
    (set) => ({
      trips: [sampleTrip1],
      selectedTripId: null,

      selectTrip: (id) => set({ selectedTripId: id }),

      addTrip: (trip) => set((state) => ({
        trips: [...state.trips, { ...trip, id: generateId(), itineraryCategories: [], bookings: [], expenses: [], packingList: [], shoppingList: [], memories: [], preparationTasks: [], wishlist: [] }]
      })),

      updateTrip: (id, tripName, tripDate, participantsCount) => set((state) => ({
        trips: state.trips.map(t => t.id === id ? { ...t, tripName, tripDate, participantsCount: participantsCount ?? t.participantsCount } : t)
      })),

      deleteTrip: (id) => set((state) => {
        const newTrips = state.trips.filter(t => t.id !== id);
        return {
          trips: newTrips,
          selectedTripId: state.selectedTripId === id ? (newTrips[0]?.id || null) : state.selectedTripId
        };
      }),

      reorderTrips: (startIndex, endIndex) => set((state) => {
        const newTrips = Array.from(state.trips);
        const [removed] = newTrips.splice(startIndex, 1);
        newTrips.splice(endIndex, 0, removed);
        return { trips: newTrips };
      }),

      addCategory: (category) => set((state) => {
        if (!state.selectedTripId) return state;
        return {
          trips: state.trips.map(trip => 
            trip.id === state.selectedTripId 
              ? { ...trip, itineraryCategories: [...trip.itineraryCategories, { ...category, id: generateId(), schedules: [] }] }
              : trip
          )
        };
      }),

      updateCategory: (categoryId, name, description) => set((state) => {
        if (!state.selectedTripId) return state;
        return {
          trips: state.trips.map(trip => 
            trip.id === state.selectedTripId 
              ? {
                  ...trip,
                  itineraryCategories: trip.itineraryCategories.map(cat => 
                    cat.id === categoryId ? { ...cat, name, description } : cat
                  )
                }
              : trip
          )
        };
      }),

      deleteCategory: (categoryId) => set((state) => {
        if (!state.selectedTripId) return state;
        return {
          trips: state.trips.map(trip => 
            trip.id === state.selectedTripId 
              ? {
                  ...trip,
                  itineraryCategories: trip.itineraryCategories.filter(cat => cat.id !== categoryId)
                }
              : trip
          )
        };
      }),

      addDaySchedule: (categoryId, date) => set((state) => {
        if (!state.selectedTripId) return state;
        return {
          trips: state.trips.map(trip => {
            if (trip.id !== state.selectedTripId) return trip;
            return {
              ...trip,
              itineraryCategories: trip.itineraryCategories.map(cat => 
                cat.id === categoryId 
                  ? { ...cat, schedules: [...cat.schedules, { id: generateId(), date, events: [] }] }
                  : cat
              )
            };
          })
        };
      }),

      updateDaySchedule: (categoryId, dayId, date) => set((state) => {
        if (!state.selectedTripId) return state;
        return {
          trips: state.trips.map(trip => {
            if (trip.id !== state.selectedTripId) return trip;
            return {
              ...trip,
              itineraryCategories: trip.itineraryCategories.map(cat => {
                if (cat.id !== categoryId) return cat;
                return {
                  ...cat,
                  schedules: cat.schedules.map(day => 
                    day.id === dayId ? { ...day, date } : day
                  )
                };
              })
            };
          })
        };
      }),

      deleteDaySchedule: (categoryId, dayId) => set((state) => {
        if (!state.selectedTripId) return state;
        return {
          trips: state.trips.map(trip => {
            if (trip.id !== state.selectedTripId) return trip;
            return {
              ...trip,
              itineraryCategories: trip.itineraryCategories.map(cat => {
                if (cat.id !== categoryId) return cat;
                return {
                  ...cat,
                  schedules: cat.schedules.filter(day => day.id !== dayId)
                };
              })
            };
          })
        };
      }),

      addEvent: (categoryId, dayId, event) => set((state) => {
        if (!state.selectedTripId) return state;
        return {
          trips: state.trips.map(trip => {
            if (trip.id !== state.selectedTripId) return trip;
            return {
              ...trip,
              itineraryCategories: trip.itineraryCategories.map(cat => {
                if (cat.id !== categoryId) return cat;
                return {
                  ...cat,
                  schedules: cat.schedules.map(day => 
                    day.id === dayId ? { ...day, events: [...day.events, { ...event, id: generateId() }] } : day
                  )
                };
              })
            };
          })
        };
      }),

      updateEvent: (categoryId, dayId, eventId, updates) => set((state) => {
        if (!state.selectedTripId) return state;
        return {
          trips: state.trips.map(trip => {
            if (trip.id !== state.selectedTripId) return trip;
            return {
              ...trip,
              itineraryCategories: trip.itineraryCategories.map(cat => {
                if (cat.id !== categoryId) return cat;
                return {
                  ...cat,
                  schedules: cat.schedules.map(day => 
                    day.id === dayId ? { 
                      ...day, 
                      events: day.events.map(e => e.id === eventId ? { ...e, ...updates } : e) 
                    } : day
                  )
                };
              })
            };
          })
        };
      }),

      deleteEvent: (categoryId, dayId, eventId) => set((state) => {
        if (!state.selectedTripId) return state;
        return {
          trips: state.trips.map(trip => {
            if (trip.id !== state.selectedTripId) return trip;
            return {
              ...trip,
              itineraryCategories: trip.itineraryCategories.map(cat => {
                if (cat.id !== categoryId) return cat;
                return {
                  ...cat,
                  schedules: cat.schedules.map(day => 
                    day.id === dayId ? { ...day, events: day.events.filter(e => e.id !== eventId) } : day
                  )
                };
              })
            };
          })
        };
      }),

      addBooking: (booking) => set((state) => {
        if (!state.selectedTripId) return state;
        return {
          trips: state.trips.map(trip => 
            trip.id === state.selectedTripId 
              ? { ...trip, bookings: [...trip.bookings, { ...booking, id: generateId() }] }
              : trip
          )
        };
      }),

      deleteBooking: (bookingId) => set((state) => {
        if (!state.selectedTripId) return state;
        return {
          trips: state.trips.map(trip => 
            trip.id === state.selectedTripId 
              ? { ...trip, bookings: trip.bookings.filter(b => b.id !== bookingId) }
              : trip
          )
        };
      }),

      updateBooking: (bookingId, updates) => set((state) => {
        if (!state.selectedTripId) return state;
        return {
          trips: state.trips.map(trip => {
            if (trip.id !== state.selectedTripId) return trip;
            return {
              ...trip,
              bookings: trip.bookings.map(b => b.id === bookingId ? { ...b, ...updates } : b)
            };
          })
        };
      }),

      updateBookingImage: (bookingId, imageKey) => set((state) => {
        if (!state.selectedTripId) return state;
        return {
          trips: state.trips.map(trip => {
            if (trip.id !== state.selectedTripId) return trip;
            return {
              ...trip,
              bookings: trip.bookings.map(b => b.id === bookingId ? { ...b, imageKey } : b)
            };
          })
        };
      }),

      addExpense: (expense) => set((state) => {
        if (!state.selectedTripId) return state;
        return {
          trips: state.trips.map(trip => 
            trip.id === state.selectedTripId 
              ? { ...trip, expenses: [...trip.expenses, { ...expense, id: generateId() }] }
              : trip
          )
        };
      }),

      updateExpense: (expenseId, updates) => set((state) => {
        if (!state.selectedTripId) return state;
        return {
          trips: state.trips.map(trip => {
            if (trip.id !== state.selectedTripId) return trip;
            return {
              ...trip,
              expenses: trip.expenses.map(e => e.id === expenseId ? { ...e, ...updates } : e)
            };
          })
        };
      }),

      deleteExpense: (expenseId) => set((state) => {
        if (!state.selectedTripId) return state;
        return {
          trips: state.trips.map(trip => 
            trip.id === state.selectedTripId 
              ? { ...trip, expenses: trip.expenses.filter(e => e.id !== expenseId) }
              : trip
          )
        };
      }),

      toggleExpensePaid: (expenseId) => set((state) => {
        if (!state.selectedTripId) return state;
        return {
          trips: state.trips.map(trip => 
            trip.id === state.selectedTripId 
              ? { ...trip, expenses: trip.expenses.map(e => e.id === expenseId ? { ...e, isPaid: !e.isPaid } : e) }
              : trip
          )
        };
      }),

      addPackingItem: (item) => set((state) => {
        if (!state.selectedTripId) return state;
        return {
          trips: state.trips.map(trip => 
            trip.id === state.selectedTripId 
              ? { ...trip, packingList: [...(trip.packingList || []), { ...item, isPacked: false, id: generateId() }] }
              : trip
          )
        };
      }),

      updatePackingItem: (itemId, updates) => set((state) => {
        if (!state.selectedTripId) return state;
        return {
          trips: state.trips.map(trip => {
            if (trip.id !== state.selectedTripId) return trip;
            return {
              ...trip,
              packingList: (trip.packingList || []).map(item => item.id === itemId ? { ...item, ...updates } : item)
            };
          })
        };
      }),

      togglePackingItem: (itemId) => set((state) => {
        if (!state.selectedTripId) return state;
        return {
          trips: state.trips.map(trip => 
            trip.id === state.selectedTripId 
              ? { ...trip, packingList: (trip.packingList || []).map(item => item.id === itemId ? { ...item, isPacked: !item.isPacked } : item) }
              : trip
          )
        };
      }),

      deletePackingItem: (itemId) => set((state) => {
        if (!state.selectedTripId) return state;
        return {
          trips: state.trips.map(trip => 
            trip.id === state.selectedTripId 
              ? { ...trip, packingList: (trip.packingList || []).filter(item => item.id !== itemId) }
              : trip
          )
        };
      }),

      addShoppingItem: (item) => set((state) => {
        if (!state.selectedTripId) return state;
        return {
          trips: state.trips.map(trip => 
            trip.id === state.selectedTripId 
              ? { ...trip, shoppingList: [...(trip.shoppingList || []), { ...item, isBought: false, id: generateId() }] }
              : trip
          )
        };
      }),

      updateShoppingItem: (itemId, updates) => set((state) => {
        if (!state.selectedTripId) return state;
        return {
          trips: state.trips.map(trip => {
            if (trip.id !== state.selectedTripId) return trip;
            return {
              ...trip,
              shoppingList: (trip.shoppingList || []).map(item => item.id === itemId ? { ...item, ...updates } : item)
            };
          })
        };
      }),

      toggleShoppingItem: (itemId) => set((state) => {
        if (!state.selectedTripId) return state;
        return {
          trips: state.trips.map(trip => 
            trip.id === state.selectedTripId 
              ? { ...trip, shoppingList: (trip.shoppingList || []).map(item => item.id === itemId ? { ...item, isBought: !item.isBought } : item) }
              : trip
          )
        };
      }),

      deleteShoppingItem: (itemId) => set((state) => {
        if (!state.selectedTripId) return state;
        return {
          trips: state.trips.map(trip => 
            trip.id === state.selectedTripId 
              ? { ...trip, shoppingList: (trip.shoppingList || []).filter(item => item.id !== itemId) }
              : trip
          )
        };
      }),

      addMemory: (memory) => set((state) => {
        if (!state.selectedTripId) return state;
        return {
          trips: state.trips.map(trip => 
            trip.id === state.selectedTripId 
              ? { ...trip, memories: [...(trip.memories || []), { ...memory, id: generateId() }] }
              : trip
          )
        };
      }),

      updateMemory: (memoryId, updates) => set((state) => {
        if (!state.selectedTripId) return state;
        return {
          trips: state.trips.map(trip => 
            trip.id === state.selectedTripId 
              ? { ...trip, memories: (trip.memories || []).map(m => m.id === memoryId ? { ...m, ...updates } : m) }
              : trip
          )
        };
      }),

      deleteMemory: (memoryId) => set((state) => {
        if (!state.selectedTripId) return state;
        return {
          trips: state.trips.map(trip => 
            trip.id === state.selectedTripId 
              ? { ...trip, memories: (trip.memories || []).filter(m => m.id !== memoryId) }
              : trip
          )
        };
      }),

      addPreparationTask: (task) => set((state) => {
        if (!state.selectedTripId) return state;
        return {
          trips: state.trips.map(trip => 
            trip.id === state.selectedTripId 
              ? { ...trip, preparationTasks: [...(trip.preparationTasks || []), { ...task, id: generateId() }] }
              : trip
          )
        };
      }),

      updatePreparationTask: (taskId, updates) => set((state) => {
        if (!state.selectedTripId) return state;
        return {
          trips: state.trips.map(trip => 
            trip.id === state.selectedTripId 
              ? { ...trip, preparationTasks: (trip.preparationTasks || []).map(t => t.id === taskId ? { ...t, ...updates } : t) }
              : trip
          )
        };
      }),

      deletePreparationTask: (taskId) => set((state) => {
        if (!state.selectedTripId) return state;
        return {
          trips: state.trips.map(trip => 
            trip.id === state.selectedTripId 
              ? { ...trip, preparationTasks: (trip.preparationTasks || []).filter(t => t.id !== taskId) }
              : trip
          )
        };
      }),

      generateAutoTasks: () => set((state) => {
        if (!state.selectedTripId) return state;
        const currentTrip = state.trips.find(t => t.id === state.selectedTripId);
        if (!currentTrip || !currentTrip.tripDate) return state;

        const tripDate = new Date(currentTrip.tripDate);
        if (isNaN(tripDate.getTime())) return state;

        const addDays = (date: Date, days: number) => {
          const result = new Date(date);
          result.setDate(result.getDate() + days);
          return result.toISOString().split('T')[0];
        };

        const autoTasks: PreparationTask[] = [
          {
            id: generateId(),
            title: '飛行機（JAL特典）予約',
            category: '飛行機',
            targetDate: addDays(tripDate, -330),
            status: 'pending',
            notes: '搭乗日の330日前の午前9:30から',
            isAutoGenerated: true
          },
          {
            id: generateId(),
            title: '飛行機（通常）予約',
            category: '飛行機',
            targetDate: addDays(tripDate, -355),
            status: 'pending',
            notes: '搭乗日の355日前の午前9:30から',
            isAutoGenerated: true
          },
          {
            id: generateId(),
            title: 'ホテル・宿の予約',
            category: 'ホテル',
            targetDate: addDays(tripDate, -180),
            status: 'pending',
            notes: '半年前（目安）',
            isAutoGenerated: true
          },
          {
            id: generateId(),
            title: 'レンタカーの予約',
            category: 'レンタカー',
            targetDate: addDays(tripDate, -90),
            status: 'pending',
            notes: '3ヶ月前（目安）。飛行機・宿確定後に手配',
            isAutoGenerated: true
          },
          {
            id: generateId(),
            title: 'プロ野球観戦チケット 予約',
            category: 'イベント',
            targetDate: addDays(tripDate, -60),
            status: 'pending',
            notes: '一般発売の約1.5〜2ヶ月前。ファンクラブ先行がある場合はさらに1〜2週間前',
            isAutoGenerated: true
          }
        ];

        return {
          trips: state.trips.map(trip => 
            trip.id === state.selectedTripId 
              ? { 
                  ...trip, 
                  // 既存の自動生成タスクをクリアして追加、手動追加分は残す
                  preparationTasks: [
                    ...(trip.preparationTasks || []).filter(t => !t.isAutoGenerated),
                    ...autoTasks
                  ] 
                }
              : trip
          )
        };
      }),

      addWishlistItem: (name) => set((state) => {
        if (!state.selectedTripId) return state;
        return {
          trips: state.trips.map(trip => 
            trip.id === state.selectedTripId 
              ? { ...trip, wishlist: [...(trip.wishlist || []), { id: generateId(), name }] }
              : trip
          )
        };
      }),

      deleteWishlistItem: (itemId) => set((state) => {
        if (!state.selectedTripId) return state;
        return {
          trips: state.trips.map(trip => 
            trip.id === state.selectedTripId 
              ? { ...trip, wishlist: (trip.wishlist || []).filter(item => item.id !== itemId) }
              : trip
          )
        };
      }),
      
    }),
    {
      name: 'hometown-trip-storage',
      storage: createJSONStorage(() => kvStorage),
    }
  )
);
